export interface CardProps {
  data: {
    title: string;
    description: string;
    image: string;
  };
}

export interface CardContainer {
  title: string;
  description: string;
  image: string;
}
